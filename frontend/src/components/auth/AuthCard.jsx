/**
 * AuthCard.jsx — backward-compat re-export.
 * The canonical AuthCard now lives in AuthComponents.jsx.
 * All existing pages that import from this file will continue to work.
 */
export { AuthCard as default } from './AuthComponents';
