Phase 1 is fully complete and verified. Let's proceed immediately to implement Phase 2 (Analytics Dashboard, Harvesting Infrastructure, and Errors Telemetry).

Please complete the following technical layers using production-grade patterns, clean error wrappers, and thorough handling of edge cases:

### 1. Database Schema Migrations (PostgreSQL 17)
Create a new migration script under `backend/migrations/` to establish the telemetry tracking ecosystem. Ensure all data collections possess the standard tracking fields (`created_at`, `updated_at`, `created_by`, `updated_by`):
- `user_sessions`: id (UUID v4), user_id (nullable, references users.id on delete set null), session_token (string, indexed), ip_address (string), country (string, default 'Unknown'), time_spent_seconds (integer, default 0), last_activity_at (timestamp with time zone).
- `system_errors`: id (UUID v4), user_id (nullable, references users.id), error_message (text), stack_trace (text), endpoint (string), created_at (timestamp with time zone).

### 2. Traffic Harvesting & Heartbeat Middleware
- Create `backend/middleware/analyticsTracker.js`. It must hook into incoming API routes non-blockingly. Parse IP configurations and assign a fallback country identifier (mock a translation routine or parse request header locales for local setup).
- Implement an active heartbeat mechanism. When a session initiates, track it via Redis using a short TTL window. Every update ping from the UI extends the Redis window and updates `last_activity_at`. Write an internal job or connection hook that flushes calculated differentials cleanly into the persistent PostgreSQL `user_sessions` collection.

### 3. Analytics Controller & Protected Admin View
- Create `backend/controllers/analyticsController.js`. It must provide data collections for the admin dashboard dashboard:
  - Total visitor traffic groupings paired by `country`.
  - Aggregated temporal matrices tracing total failed logins vs server exceptions.
  - Average user engagement retention timelines (time spent).
- Expose these arrays through `backend/routes/analyticsRoutes.js` via a protected GET `/api/analytics/dashboard` route. Ensure the token extraction verification layer confirms an admin role flag (or static email checks against your initial seed values like 'admin@locator.com').

### 4. Integration & Test Suites
- Update `backend/index.js` to hook up the analytics routers and link the error-tracking capture logic directly inside your global `errorHandler.js` script so exceptions automatically populate the `system_errors` table.
- Generate standalone integration verification routines under `backend/__tests__/analytics.test.js` to guarantee data layers capture telemetry correctly and match your required strict JSON output configuration. Run the test suite and confirm it passes with 100% execution success.